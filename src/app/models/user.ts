export class User {
  autoId: number;
  firstName: string;
  lastName: string;
  emailId: string;
  password: string;
  createdDate: Date;
  createdBy: number;
  role: number;
  contactNumber: string;
  active: boolean;
  rollNumber: string;
  batch:number;
}
